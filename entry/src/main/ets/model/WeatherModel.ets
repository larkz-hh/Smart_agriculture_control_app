export interface WeatherInfo {
  location: string;
  condition: string;
  currentTemp: number;
  highTemp: number;
  lowTemp: number;
  precipitation: string;
  humidity: string;
  wind: string;
  light: string;
}